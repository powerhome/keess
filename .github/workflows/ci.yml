name: CI

on:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Test
        run: go test $(go list ./... | grep -v keess/tests/e2e)

      - name: Detect changed files
        id: changes
        run: |
          BASE="${{ github.event.pull_request.base.sha }}"
          HEAD="${{ github.event.pull_request.head.sha }}"
          CHANGED=$(git diff --name-only "$BASE" "$HEAD")
          DOCKER_CHANGES=$(echo "$CHANGED" | grep '^Dockerfile' | grep -v '\.dockerignore$' || true)
          CHART_CHANGES=$(echo "$CHANGED" | grep '^chart/' || true)
          if [ -n "$DOCKER_CHANGES" ]; then
            echo "has_dockerfiles=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_dockerfiles=false" >> "$GITHUB_OUTPUT"
          fi
          if [ -n "$CHART_CHANGES" ]; then
            echo "has_chart=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_chart=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Detect runner architecture
        if: steps.changes.outputs.has_dockerfiles == 'true'
        id: arch
        run: echo "goarch=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')" >> "$GITHUB_OUTPUT"

      - name: Build keess binary for Docker build validation
        if: steps.changes.outputs.has_dockerfiles == 'true'
        uses: goreleaser/goreleaser-action@f06c13b6b1a9625abc9e6e439d9c05a8f2190e94 # v7.2.3
        with:
          distribution: goreleaser
          version: "~> v2"
          args: build --single-target --snapshot --clean --id keess -o bin/keess-linux-${{ steps.arch.outputs.goarch }}
        env:
          GOOS: linux
          GOARCH: ${{ steps.arch.outputs.goarch }}

      - name: Docker build
        if: steps.changes.outputs.has_dockerfiles == 'true'
        run: |
          BASE="${{ github.event.pull_request.base.sha }}"
          HEAD="${{ github.event.pull_request.head.sha }}"
          KEESS_BIN="bin/keess-linux-${{ steps.arch.outputs.goarch }}"
          git diff --name-only "$BASE" "$HEAD" | grep '^Dockerfile' | grep -v '\.dockerignore$' | while read -r f; do
            echo "Building $f"
            if [ "$f" = "Dockerfile" ]; then
              docker build -f "$f" --build-arg KEESS_BIN="$KEESS_BIN" .
            else
              docker build -f "$f" .
            fi
          done

      - name: Install Helm
        if: steps.changes.outputs.has_chart == 'true'
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1

      - name: Helm lint
        if: steps.changes.outputs.has_chart == 'true'
        run: helm lint chart/

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          version: v2.12.2
