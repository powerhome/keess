{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>powerhome/renovate-config",
    "local>powerhome/renovate-config:ci-kubed-read-token",
    "group:allNonMajor"
  ],
  "packageRules": [
    {
      "description": "Never pin our own images in the chart: chart/values.yaml leaves image.tag empty so appVersion selects the build, and a pin here silently overrides that (it shipped a 1.3.3 image under a 1.4.2 appVersion for months). Scoped to the chart so Renovate still manages these images anywhere else.",
      "matchFileNames": [
        "chart/values.yaml"
      ],
      "matchPackageNames": [
        "powerhome/keess",
        "powerhome/keess-kubeconfig-reloader"
      ],
      "enabled": false
    }
  ]
}
